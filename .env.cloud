# EchoTrail Cloud Environment Configuration
# For deployment with external managed services (Neon PostgreSQL, etc.)
# WARNING: Do not commit this file with real secrets!

# External Database Configuration (Neon PostgreSQL)
EXTERNAL_DATABASE_URL=postgresql://username:password@ep-xxx-xxx.us-east-1.aws.neon.tech/echotrail?sslmode=require

# External Redis Configuration (Optional - Redis Cloud, AWS ElastiCache, etc.)
EXTERNAL_REDIS_URL=redis://username:password@redis-endpoint:port

# JWT Configuration
JWT_SECRET=your-production-jwt-secret-at-least-32-characters
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# Security Configuration
BCRYPT_ROUNDS=12

# Rate Limiting
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=900000

# CORS Configuration
CORS_ORIGIN=https://yourdomain.com,https://app.yourdomain.com

# API Configuration
LOG_LEVEL=info
SWAGGER_ENABLED=false
ENABLE_RATE_LIMITING=true

# Frontend Integration
FRONTEND_URL=https://app.yourdomain.com
API_URL=https://api.yourdomain.com

# Monitoring Configuration
GRAFANA_ADMIN_PASSWORD=secure-grafana-password
ENABLE_METRICS=true

# Cloud Provider Settings
CLOUD_PROVIDER=aws
REGION=us-east-1

# Optional: File Storage Configuration
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_S3_BUCKET=echotrail-uploads
# AWS_S3_REGION=us-east-1
