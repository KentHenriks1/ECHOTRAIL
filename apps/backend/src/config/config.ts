/**
 * EchoTrail Backend Configuration
 * Simple re-export of validated environment variables
 */

// Re-export the validated environment configuration
export { env as config } from '../utils/env-validator';
